jQuery(function($) {
  // $( "code" ).each(function() {
  //   preview = $( this ).clone().html().replace(/</g, '&lt;').replace(/>/g, '&gt;');
  //   $(this).html(preview);
  // });

  // $('button').click(function () {
  //   $(this).siblings('code').toggleClass('hide');
  // });
});